services:
  api:
    image: riviamigo-api:${IMAGE_TAG:-latest}
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      S3_ENDPOINT: ${S3_ENDPOINT}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
      RUST_LOG: info
      RIVIAMIGO_ENV: production
      COOKIE_INSECURE:   # must be absent in production — explicitly cleared here
    depends_on:
      timescaledb:
        condition: service_healthy
    networks:
      - internal
      - proxy
    # Do NOT expose port directly in prod — nginx handles TLS termination
    expose:
      - "3001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    # Security: drop all Linux capabilities, read-only FS, no new privs
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      # /tmp covers the default BACKUP_ARTIFACT_DIR (/tmp/riviamigo-backups).
      # If you override BACKUP_ARTIFACT_DIR to a path outside /tmp, add it here
      # as an additional tmpfs mount (or use a named volume).
      - /tmp
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          memory: 128M

  timescaledb:
    image: timescale/timescaledb:2.16.1-pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: riviamigo
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - internal
    # NOT exposed to host — only accessible via internal network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d riviamigo"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          memory: 512M

  nginx:
    image: riviamigo-web:${IMAGE_TAG:-latest}
    build:
      context: ..
      dockerfile: infra/nginx/Dockerfile
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # TLS certificates are the only host-supplied runtime secret; everything
      # else (nginx config + web app) is baked into the image at build time.
      - /etc/ssl/riviamigo:/etc/ssl/riviamigo:ro
    networks:
      - internal
      - proxy
    depends_on:
      - api
    # Read-only root FS: nginx needs a writable /tmp for temp files and
    # /var/run for the pid file.
    read_only: true
    tmpfs:
      - /tmp
      - /var/run
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
        reservations:
          memory: 32M

volumes:
  pgdata:

networks:
  internal:
    driver: bridge
    internal: true
  proxy:
    driver: bridge
