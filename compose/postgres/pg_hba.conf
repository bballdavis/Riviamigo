# PostgreSQL Client Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     peer
host    riviamigo       riviamigo       127.0.0.1/32            scram-sha-256
host    riviamigo       riviamigo       ::1/128                 scram-sha-256
# Deny all other connections (no 0.0.0.0 or md5 fallback)
