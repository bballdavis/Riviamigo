# GENERATED FILE — DO NOT EDIT DIRECTLY.
# Source: compose/docker-compose.yml
# Generator: scripts/generate-synology-compose.mjs
services:
  riviamigo-init:
    image: ${RIVIAMIGO_IMAGE_REGISTRY:-ghcr.io/bballdavis}/riviamigo:${IMAGE_TAG:-latest}
    user: 0:0
    env_file:
      - ${RIVIAMIGO_ENV_FILE:-.env.synology}
    entrypoint:
      - /app/production-init.sh
    volumes:
      - ${RIVIAMIGO_DATA_DIR:?Set RIVIAMIGO_DATA_DIR to an absolute Synology path}/backups:/backups
      - ${RIVIAMIGO_DATA_DIR:?Set RIVIAMIGO_DATA_DIR to an absolute Synology path}/cache:/data/cache
    networks:
      - internal
    restart: no
  riviamigo:
    image: ${RIVIAMIGO_IMAGE_REGISTRY:-ghcr.io/bballdavis}/riviamigo:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - ${RIVIAMIGO_ENV_FILE:-.env.synology}
    environment:
      ALLOW_INSECURE_LAN_HTTP_AUTH: ${ALLOW_INSECURE_LAN_HTTP_AUTH:-false}
    depends_on:
      riviamigo-init:
        condition: service_completed_successfully
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - 127.0.0.1:${RIVIAMIGO_ORIGIN_PORT:-8080}:8080
    user: 1001:1001
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:rw,nosuid,nodev,size=128m
    networks:
      - internal
      - proxy
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:8080/health
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    volumes:
      - ${RIVIAMIGO_DATA_DIR:?Set RIVIAMIGO_DATA_DIR to an absolute Synology path}/backups:/backups
      - ${RIVIAMIGO_DATA_DIR:?Set RIVIAMIGO_DATA_DIR to an absolute Synology path}/cache:/data/cache
    deploy:
      resources:
        limits:
          memory: 640M
        reservations:
          memory: 160M
  timescaledb:
    image: timescale/timescaledb:2.28.3-pg18@sha256:2c718e700b1c75b93488085596f2254c991c3aa29584fc282ebbe62fd8335791
    restart: unless-stopped
    environment:
      POSTGRES_DB: riviamigo
      POSTGRES_USER: ${POSTGRES_USER:-riviamigo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      PGDATA: /db
    volumes:
      - ${RIVIAMIGO_DATA_DIR:?Set RIVIAMIGO_DATA_DIR to an absolute Synology path}/db:/db
    networks:
      - internal
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U ${POSTGRES_USER:-riviamigo} -d riviamigo
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5m
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
  redis:
    image: redis:8.8.0-alpine@sha256:9d317178eceac8454a2284a9e6df2466b93c745529947f0cd42a0fa9609d7005
    restart: unless-stopped
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      - --appendonly
      - yes
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
    volumes:
      - ${RIVIAMIGO_DATA_DIR:?Set RIVIAMIGO_DATA_DIR to an absolute Synology path}/redis:/data
    networks:
      - internal
    healthcheck:
      test:
        - CMD-SHELL
        - redis-cli --no-auth-warning -a "$$REDIS_PASSWORD" ping
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M
networks:
  internal:
    driver: bridge
    internal: true
  proxy:
    driver: bridge
